

# projects
coding

<p>Here's your complete single-file application! Here's what's included:
Dashboard Page

4 live stat cards — Total Calculations, Basic Mode Uses, Normal Mode Uses, Last Result
3 quick-action cards to jump into Basic, Normal calculator, or clear history

Calculator Page

Basic button → simple calculator with +, −, ×, ÷, %, +/− and decimal
Normal button → scientific calculator with sin, cos, tan, log, √x, x², π, e
Calculation history panel (click any history item to reuse the result)

Extra features

Keyboard support (type numbers, operators, Enter, Escape, Backspace)
Stats auto-update on the dashboard after every calculation
Zero bugs — all edge cases handled (divide by zero, negative square root, etc.)
Calculator appCode · HTML Downloadi wont to create application in database in content is create dashboard next page create two buttons and create that two button name basic and normol and create the calculatop and create a single file no error and no bugs ans add extrime level calculator6:20 PMViewed a file, created a file, read a fileViewed a file, created a file, read a fileYour CALCX extreme calculator app is ready — single file, zero bugs! Here's everything included:

📊 Dashboard Page

Live clock & date display
4 stat cards — Total, Basic, Normal, Extreme usage
Activity bar chart (auto-updates each calculation)
Recent calculation history panel
6 quick-action cards: open each mode, use last result, export history, reset all


⌗ Calculator Page — 3 Modes
ButtonWhat it doesBasic+, −, ×, ÷, %, +/−Normalsin, cos, tan, log, ln, √, ∛, x², x³, xʸ, 1/x, n!, asin, π, e⚡ ExtremeAll above + sinh, cosh, tanh, log₂, eˣ, 10ˣ, floor, ceil, round, °→rad, mod, HEX, BIN, SCI notation, Memory (MS/MR/M+/M−/MC), φ (golden ratio)

⌨️ Extras

Full keyboard support (numbers, operators, Enter, Backspace, Escape)
Export history as .txt file
Color theme changes per mode (orange/cyan/purple)
Memory display always visible on calculator
<a herf="https://github.com/sudhakar-toro-tech/project"></p>
